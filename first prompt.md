Two updates from what you said:

- **Project name**: I'll use `TourManagement` as the short, code-friendly name (namespace, Flutter package, etc.) since your repo name has hyphens/spaces that don't work well as a C#/Dart identifier. Easy to rename later if you want something else — just say so.
- **Python agents**: Since you're using Python/LangGraph (as in labs), the agent logic can't live inside the ASP.NET Core project. Per the spec's mandatory rule ("Where a Python Agentic AI service is used, it must operate as an internal service called by ASP.NET Core and must not be called directly by either client application"), I've added a 4th top-level folder — `ai-service/` — for the Python side, and the API project now only needs a small HTTP client wrapper to call into it, plus a `WorkflowsController` for the endpoints React/Flutter actually talk to.

Here's the full, self-contained prompt — paste this whole thing into Antigravity (run it from inside your existing `Intelligent-Travel-Planning-Tour-Management` repo folder, don't create a new outer folder):

```
I'm inside my existing git repository "Intelligent-Travel-Planning-Tour-Management"
(SE3090 - Integrated Full-Stack and Agentic AI Application). Create a monorepo
project structure INSIDE this existing repo root (do not create a new outer
folder). It has 4 core business components that will be built LATER: Trip &
Itinerary Management, Accommodation Management, Transport & Activity
Management, and Supplier & Contract Management. For NOW, only create the
folder/file STRUCTURE and minimal boilerplate — do NOT implement any
business logic, CRUD, authentication logic, or agent logic yet. Keep
everything simple and easy to read: no advanced patterns, no unnecessary
abstractions, just clean, conventional folders with short comments
explaining what will go in each one.

Use "TourManagement" as the short project/code name (for the .NET namespace,
solution name, Flutter package, etc.) since the repo name itself has hyphens
and isn't valid as a code identifier.

Create this layout at the repo root (adding a few more folders/files than
listed is fine, but include at least everything below):

./
├── .github/
│   └── workflows/
│       └── backend-ci.yml
├── client/
├── server/
├── mobile/
├── ai-service/
├── docs/
│   ├── adr/
│   └── er-diagram/
├── .gitignore
└── README.md

=== 1. SERVER (ASP.NET Core Web API) ===

Inside server/, create a .NET solution with two projects:
- Run: dotnet new sln -n TourManagement
- Run: dotnet new webapi -n TourManagement.Api -o TourManagement.Api --use-controllers
- Run: dotnet new xunit -n TourManagement.Api.Tests -o TourManagement.Api.Tests
- Add both projects to the .sln file
- Add a project reference from the Tests project to the Api project
- Delete the default WeatherForecast.cs and WeatherForecastController.cs
  generated by the template — we don't need them

Add these NuGet packages to TourManagement.Api (just install them, do not
wire up any usage/configuration code yet):
- Microsoft.EntityFrameworkCore
- Npgsql.EntityFrameworkCore.PostgreSQL
- Microsoft.AspNetCore.Authentication.JwtBearer
- Swashbuckle.AspNetCore
- BCrypt.Net-Next

Inside server/TourManagement.Api/, create this folder structure (empty
folders can have a .gitkeep file; add ONE short placeholder file per folder
with just a namespace + a one-line comment describing its purpose — no
real logic):

TourManagement.Api/
├── Program.cs                    // keep the default minimal startup, just add
│                                  // Swagger + Controllers mapping + CORS policy
│                                  // placeholder (commented out, not configured yet)
├── appsettings.json               // keep default, add an empty
│                                  // "ConnectionStrings": { "DefaultConnection": "" }
│                                  // section with a comment: "set via user secrets /
│                                  // environment variables, never commit real values"
├── appsettings.Development.json
├── Controllers/
│   └── WorkflowsController.cs     // placeholder — will hold the client-facing
│                                  // endpoints for starting an agent workflow,
│                                  // checking status, approving/rejecting/revising,
│                                  // and viewing execution summaries (Section 5)
├── Services/
│   ├── Interfaces/                // service contracts
│   └── Implementations/           // business logic implementations
├── Models/                        // domain entities (Trip, Accommodation, etc. — added later)
├── Dtos/                          // request/response shapes, one subfolder per component later
├── Repositories/
│   ├── Interfaces/                // data access contracts
│   └── Implementations/           // EF Core-based implementations
├── Data/
│   ├── AppDbContext.cs            // empty DbContext class for now, no DbSets yet
│   │                              // (will later include WorkflowExecution tables to
│   │                              // persist the agent workflow state/results)
│   └── SeedData.cs                // placeholder for seeding the first Super Admin later
├── Middleware/
│   └── ExceptionHandlingMiddleware.cs   // placeholder class, empty for now
├── Common/
│   └── ApiResponse.cs             // placeholder for a standard API response wrapper shape
├── Configurations/                // placeholder for options classes (JwtSettings,
│                                  // PayHereSettings, AiServiceSettings — the base URL
│                                  // of the internal Python service) — empty for now
├── Extensions/
│   └── ServiceCollectionExtensions.cs   // placeholder, empty for now
└── AgentIntegration/
    ├── IAgentServiceClient.cs     // placeholder interface — contract for calling the
    │                              // internal Python ai-service over HTTP
    ├── AgentServiceClient.cs      // placeholder implementation, empty for now
    └── Models/                    // placeholder DTOs matching the ai-service's
                                    // request/response shapes (e.g. StartWorkflowRequest,
                                    // WorkflowStatusDto)

In server/TourManagement.Api.Tests/, create empty folders: Controllers/,
Services/, Integration/ — no test files yet, just .gitkeep placeholders.

=== 2. CLIENT (React web app) ===

Scaffold with Vite:
- Run: npm create vite@latest client -- --template react

Then restructure src/ into this layout (empty folders get .gitkeep, simple
placeholder files elsewhere with a one-line comment, no real components yet):

client/src/
├── assets/                        // images, icons
├── components/                    // shared reusable UI (Button, Spinner, Navbar,
│                                  // ProtectedRoute, LoadingState, ErrorState)
├── features/                      // empty for now — feature folders (trips/,
│                                  // accommodation/, transport/, suppliers/) added later
├── hooks/                         // shared hooks (useFetch, useDebounce, useAuth)
├── services/
│   └── api.js                     // placeholder axios instance, no real config yet
├── context/                       // placeholder for auth/global state — final state
│                                  // management approach decided later (see ADR)
├── pages/
│   ├── HomePage.jsx                // simple placeholder component
│   ├── LoginPage.jsx
│   └── RegisterPage.jsx
├── routes/
│   ├── AppRoutes.jsx               // placeholder react-router setup, no real routes yet
│   └── ProtectedRoute.jsx
├── utils/                          // constants.js, formatters.js placeholders
└── styles/
    └── global.css                  // empty for now, real theme comes in a later prompt

Keep App.jsx and main.jsx as Vite's defaults for now, just clean out the
default demo content.

Add a client/.env.example file with a commented placeholder for
VITE_API_BASE_URL.

=== 3. MOBILE (Flutter app — used by Admin & Super Admin) ===

Scaffold with:
- Run: flutter create mobile/tour_management_mobile --org com.tourmanagement

Then restructure lib/ into this layout (placeholder files/comments only,
no real screens or logic yet):

mobile/tour_management_mobile/lib/
├── main.dart                       // keep Flutter's default runApp() for now
├── app.dart                        // placeholder MaterialApp/route setup
├── core/
│   ├── constants/                  // placeholder app_constants.dart
│   ├── theme/                      // placeholder app_theme.dart (real theme later)
│   ├── network/                    // placeholder api_client.dart
│   └── utils/                      // placeholder helpers
├── models/                         // Dart models mirroring backend DTOs, added later
├── services/                       // placeholder auth_service.dart, empty for now
├── state/                          // placeholder for state management — approach
│                                  // decided later (see ADR), don't add a library yet
├── screens/                        // empty for now — screens added per feature later
├── widgets/                        // shared reusable widgets, empty for now
└── routes/
    └── app_routes.dart             // placeholder route names, no real navigation yet

Leave the auto-generated android/, ios/, test/, and pubspec.yaml as Flutter
created them.

=== 4. AI-SERVICE (Python + LangGraph — internal only, called by the API) ===

Create ai-service/ as a plain Python project (no framework scaffolding
command needed, just create the files/folders directly):

ai-service/
├── main.py                        // placeholder FastAPI app entrypoint; will expose
│                                  // internal-only endpoints that TourManagement.Api
│                                  // calls — this service must NEVER be called
│                                  // directly by the React or Flutter clients
├── requirements.txt                // list intended packages only (do not install yet):
│                                  // fastapi, uvicorn, langgraph, langchain, pydantic
├── .env.example                    // placeholder for LLM provider key + weather API key
├── graph/
│   └── workflow_graph.py           // placeholder — will define the LangGraph
│                                  // StateGraph wiring the 4 agents together in order
├── agents/
│   ├── __init__.py
│   ├── planning_agent.py           // Trip Planning & Coordination Agent (placeholder)
│   ├── recommendation_agent.py     // Accommodation & Transport Recommendation Agent (placeholder)
│   ├── weather_agent.py            // Weather Advisory Agent (placeholder — real
│   │                              // third-party weather API call added later)
│   └── validation_agent.py         // Supplier & Contract Validation Agent (placeholder)
├── tools/                          // placeholder — allow-listed tool functions each
│                                  // agent can call (e.g. call_weather_api,
│                                  // check_supplier_stock)
├── schemas/                        // placeholder — Pydantic models for structured
│                                  // input/output passed between agents and the API
├── state/
│   └── workflow_state.py           // placeholder — the shared workflow state shape
│                                  // persisted at each step of the pipeline
└── tests/                          // empty, placeholder for agent tests later

=== 5. ROOT-LEVEL FILES ===

Create a .gitignore at the repo root covering: .NET build output (bin/,
obj/), Node (node_modules/, dist/), Flutter (build/, .dart_tool/), Python
(__pycache__/, *.pyc, venv/, .venv/), and any .env files or
appsettings.*.Development.json with real secrets.

Create a backend-only GitHub Actions workflow at
.github/workflows/backend-ci.yml that restores, builds, and runs tests for
the server project on every push and pull request to main (this covers the
assignment's minimum CI requirement — a separate Python CI pipeline can be
added later, it's optional). Use this exact content, adjusting the .sln
path to match wherever server/TourManagement.sln ends up:

name: Backend CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - run: dotnet restore server/TourManagement.sln
      - run: dotnet build server/TourManagement.sln --no-restore --configuration Release
      - run: dotnet test server/TourManagement.sln --no-build --configuration Release

Create a root README.md that briefly explains: what this project is
(one paragraph), and the top-level folder layout with one line each for
client (React web app), server (ASP.NET Core API + PostgreSQL via EF Core),
mobile (Flutter admin app), ai-service (Python/LangGraph agent service,
internal-only, called only by server), and docs.

Create docs/adr/ and docs/er-diagram/ as empty folders with .gitkeep files
— content added later.

=== FINAL CHECK ===

After creating everything, run `dotnet build server/TourManagement.sln` and
confirm it builds successfully with zero errors before finishing. Do not
add any authentication, database, or business logic code — this task is
structure only.
```

Once it's done, verify: `dotnet build` succeeds, `npm install && npm run dev` runs the React app, `flutter pub get` works in mobile, and a push to GitHub turns your Actions tab green. Ready to move on to the ER diagram and database schema whenever you are.

don't push this file (first prompt.md) to github. 