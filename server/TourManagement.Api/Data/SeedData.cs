namespace TourManagement.Api.Data;

// SeedData — placeholder
// Will seed the first Super Admin user into the database when the
// authentication system is implemented. Called from Program.cs on startup.

public static class SeedData
{
    // SeedAsync(IServiceProvider serviceProvider) will be added later.
}
