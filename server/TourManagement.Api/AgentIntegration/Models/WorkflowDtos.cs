namespace TourManagement.Api.AgentIntegration.Models;

// Placeholder DTOs that mirror the ai-service's request/response shapes.
// These will grow as the agent endpoints are defined in the Python service.

// Request sent to ai-service to start a new workflow.
public class StartWorkflowRequest
{
    // Properties added later (e.g. TripId, UserId, Parameters).
}

// Response / status DTO returned by the ai-service.
public class WorkflowStatusDto
{
    // Properties added later (e.g. WorkflowId, Status, Steps, Result).
}
