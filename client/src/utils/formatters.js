// utils/formatters.js — placeholder
// Will hold shared formatting helpers (dates, currency, duration, etc.)

// export function formatDate(dateStr) { ... }
// export function formatCurrency(amount, currency = 'LKR') { ... }
