// services/api.js — placeholder axios instance
// Real base URL, interceptors, and auth header injection will be configured
// in a later prompt once the backend API is running.

// import axios from 'axios';

// const api = axios.create({
//   baseURL: import.meta.env.VITE_API_BASE_URL,
//   headers: { 'Content-Type': 'application/json' },
// });

// export default api;
