// routes/AppRoutes.jsx — placeholder react-router-dom setup
// Real routes (with feature pages and ProtectedRoute guards) will be added
// in the routing/auth prompt.

// import { BrowserRouter, Routes, Route } from 'react-router-dom';
// import HomePage from '../pages/HomePage';
// import LoginPage from '../pages/LoginPage';
// import RegisterPage from '../pages/RegisterPage';
// import ProtectedRoute from './ProtectedRoute';

export default function AppRoutes() {
  // Route tree will be built here once react-router-dom is installed.
  return null;
}
