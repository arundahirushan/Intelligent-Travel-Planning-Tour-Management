// routes/ProtectedRoute.jsx — placeholder
// Will redirect unauthenticated users to /login.
// Real implementation depends on auth state (context / store) decided later.

export default function ProtectedRoute({ children }) {
  // Auth check + redirect will be added in the auth prompt.
  return children;
}
