// main.dart -- placeholder Flutter entry point
// Keep Flutter's default runApp() for now; real app setup in app.dart later.
import 'package:flutter/material.dart';
import 'app.dart';

void main() {
  runApp(const App());
}
